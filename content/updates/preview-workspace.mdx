## What changed

The prototype now treats preview as a workspace rather than a loading screen.
Every section has a title, output size, conversion mode and warning state.

Users can reorder, remove, restore, split and merge sections before opening the
sample download. Drag and drop has button-based keyboard alternatives.

This is still deterministic demo data. The separate backend will later provide
real preview manifests and short-lived artifact links.
