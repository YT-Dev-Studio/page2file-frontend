import Image from "next/image";
import type { ReactNode } from "react";

export const ChromeIcon = (): ReactNode => (
  <Image
    alt=""
    height={24}
    src="/brand/chrome.svg"
    unoptimized
    width={24}
  />
);
